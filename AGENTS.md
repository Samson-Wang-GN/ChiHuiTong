# AGENTS.md

## 适用范围与优先级

本文件是“齿慧通”项目级开发规范，适用于仓库内所有代码、文档、脚本、配置和发布工作。它优先于机器全局规范；子目录如有更具体的 `AGENTS.md`，仅在其作用域内覆盖本文件。

## 产品与工程原则

- 所有 Web 后台原型与正式前端必须遵循 `docs/design-requirements.md` 的 Arco Design 基线；优先使用官方组件，不自行仿制或混用其他设计系统。
- 四类后台分别维护独立入口与角色页面组；角色页面不能拼接为一个跨角色工作台。可共享基础组件和设计变量。

- “齿慧通”面向真实口腔医疗业务，默认按医疗信息系统的风险等级设计。
- 用户负责说明业务目标；Codex 负责选择简单、稳定、可维护的技术方案，并把重要取舍写入 `docs/decisions/`。
- 先澄清业务规则和验收标准，再实现代码。未明确的产品细节可采用保守默认值，但必须在需求文档中标记“待确认”。
- 不做需求之外的大范围重构，不为未来可能出现的需求提前构建复杂抽象。
- 优先复用仓库现有技术栈、模块边界和交互模式；引入依赖或新技术栈必须说明收益、代价、安全影响和退出方案。
- 后台界面优先信息密度、操作效率、状态清晰和可追溯性；默认中文，并保证移动端基本可用。

## 开始任务前

开发人员和 Codex 必须依次阅读：

1. 本文件；
2. `README.md`；
3. `docs/requirements.md`；
4. `docs/architecture.md`；
5. `docs/tasks.md`；
6. 与任务相关的 `docs/decisions/` 和模块文档。

前端和原型任务还必须阅读 `docs/design-requirements.md` 与 `docs/prototype-review.md`。

开始修改前检查 `git status`，识别并保留用户或其他开发者的未提交改动。不得覆盖、回滚或顺带格式化无关文件。

## 需求与计划管理

- 新需求先更新 `docs/requirements.md`，至少记录目标、用户、范围、不做什么、业务规则、数据与权限影响、验收标准和待确认项。
- 较大功能先在 `docs/tasks.md` 拆成可以独立验证的小任务；同一时间只推进少量进行中事项。
- 影响系统边界、数据模型、安全、依赖、部署或长期维护成本的选择，使用 `docs/decisions/NNNN-title.md` 记录背景、备选方案、决定、取舍和后果。
- 需求变更不得只留在聊天、口头或提交信息中；必须同步修改项目文档。
- 每项需求、任务、提交和发布记录应能相互追踪，建议使用稳定编号，如 `REQ-001`、`TASK-001`、`ADR-0001`。

## 实现规范

- 按“可运行、可验证的垂直切片”小步实现；每一步应包含必要的业务逻辑、权限、错误处理、日志和测试。
- 服务端是权限和业务规则的最终执行边界；前端校验仅用于改善交互。
- 所有外部输入必须进行类型、格式、范围和权限校验；错误必须可追踪、对用户可理解、在可恢复场景下可重试。
- 写操作默认考虑事务、一致性、幂等和重复提交；异步任务默认考虑重试、超时、去重和失败补偿。
- 外部系统集成必须通过明确的适配边界，记录授权范围、用途、频率、超时、重试、审计和降级策略；不得绕过技术控制或擅自调用未获授权的接口。
- 禁止吞异常、提交调试输出、明显 TODO、死代码、真实密钥、生产数据样本或无用生成物。
- 公共逻辑应有清晰边界，但只有出现真实复用需求时才抽象。

## 医疗数据与安全

- 患者身份、联系方式、病历、影像、诊疗、预约及支付信息默认视为敏感数据。
- 遵循最小权限、最少采集、目的限定、租户隔离和最短保存原则；访问控制必须在服务端执行。
- 日志、异常、分析数据和测试夹具不得包含明文敏感信息、密码、Token、Cookie、私钥或完整第三方响应。
- 生产数据不得直接复制到开发或测试环境；确有必要时必须经过授权和不可逆脱敏。
- 高风险操作应具有权限校验、二次确认、幂等保护和审计记录；批量删除、导出、授权变更和外发属于高风险操作。
- `.env.example` 只列变量名和安全示例；真实秘密只存于未提交的 `.env` 或部署平台的密钥管理系统。
- 涉及新的个人信息用途、跨租户处理、外部共享、自动化决策或模型训练时，必须先完成专项评审，不得由普通功能需求隐含授权。

## 测试与验证

- 用户已确定：所有测试（单元、集成、端到端、浏览器、启动冒烟和验收）只能在公共开发测试服务器 `140.143.125.233` 上执行。本地只维护代码/文档和Git、进行静态文件与配置检查，不运行本地应用或浏览器测试；GitHub托管Runner也不作为测试环境。
- 本地 `D:\My work\ChiHuiTong` 是 Git 工作仓库/镜像，负责与 `Samson-Wang-GN/ChiHuiTong` 通信；服务器不建立该项目 `.git`、不配置GitHub凭据、不执行clone/pull/push。使用按提交打包并校验SHA-256的SSH传输。
- 服务器项目目录 `/home/ubuntu/ChiHuiTong`；代码版本、独立测试环境和测试产物分离。禁止修改齿慧公共服务、Study System、现有数据库/Redis及共享Nginx配置来迁就本项目；未经另行批准不开放新的公网端口。具体流程见 `docs/operations/development.md`。

- 2026-09-12专项授权例外（REQ-044）：允许在现有开发HTTPS站点增加且仅维护 `/chihuitong/` 的独立受保护验收路由和专属include，先备份/检查再平滑重载；不得改动其他既有路由，不新增公网端口。允许本项目独立验收库、应用服务与任务timer；其他共享服务及数据边界不变。操作和回退见 `docs/operations/acceptance.md`。

- 每个功能必须有自动化测试；暂时无法自动化时，必须在任务文档中写出可复现的人工验证步骤、预期结果和限制。
- 优先测试业务规则、权限边界、租户隔离、异常路径、重复提交、数据迁移和外部依赖失败。
- 提交前按项目实际工具尽可能运行：格式化、lint、类型检查、单元测试、集成测试、迁移检查、构建和启动冒烟测试。
- 不得以“整体任务成功”代替关键子步骤验证；每个高风险环节都要有独立证据。
- 未执行或未通过的验证必须在最终交付中明确说明原因、影响和后续动作。

## Git 与评审

- 一个稳定功能点一个提交；提交前检查差异范围和敏感信息。
- Commit message 使用 `feat:`、`fix:`、`docs:`、`refactor:`、`test:`、`chore:` 前缀。
- 禁止未经用户明确要求使用 `git reset --hard`、强制推送或改写共享历史。
- 合并请求应说明关联需求、变更内容、验证证据、数据/权限影响、兼容性、发布与回滚方案。
- 至少一名非作者评审高风险改动；认证授权、医疗数据、迁移、支付、批量操作和生产发布不得仅靠自审进入生产。

## 发布与运维

- 环境至少区分开发、测试/预发布和生产；禁止用生产环境代替功能测试。
- 发布采用最小变更集，发布前确认备份、迁移、配置、依赖、静态资源、任务队列和回滚步骤。
- 数据库迁移必须可审查；破坏性迁移使用“扩展—迁移—收缩”方式分阶段实施。
- 发布后验证服务状态、关键页面、关键接口、异步任务、错误日志和核心业务闭环；“进程存活”不等于业务可用。
- 生产修复坚持先备份、后替换，保留版本、时间、执行人、校验结果和恢复位置。
- 事故优先止损和保全证据，随后记录影响范围、时间线、根因、恢复过程和防复发措施。

## 文档维护

- `README.md`：项目目标、状态、启动方式、环境变量、常用命令和部署入口。
- `docs/requirements.md`：需求、业务规则、验收标准和待确认项。
- `docs/architecture.md`：架构、模块边界、数据、集成、安全和部署结构。
- `docs/tasks.md`：当前任务、状态、验证证据、风险和下一步。
- `docs/development-management.md`：完整研发和软件管理流程。
- `docs/decisions/`：关键技术决策及其取舍。

代码、配置或流程变化时，同一任务内同步更新相关文档，避免把过期说明留给后续开发。

## 完成定义

只有同时满足以下条件，任务才可标记完成：需求与验收标准明确；功能可运行；权限和异常路径已处理；测试或可复现验证已通过；文档已同步；无敏感信息和无关改动；发布与回滚影响已说明；剩余风险和待确认项已显式记录。
