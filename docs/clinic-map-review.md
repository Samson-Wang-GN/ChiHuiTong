# 门诊资料与审核对照地图交付

2026-09-13；REQ-032/033、TASK-096；正式Web验收环境，不涉及小程序或历史localStorage原型。

## 交互与数据边界

- 详情“当前生效位置”自动显示真实地图；尚无定位但已有确认位置的待审申请时，明确“新位置已确认，等待平台审核；当前生效资料尚无定位”，不要求重复定位。另展示本次申请位置及“尚未生效”提示。
- 审核对照页并列“原资料位置 / 本次申请位置”两版真实地图。无坐标的版本显示空态，不用另一版坐标填充；已通过/已退回申请分别说明状态，历史版本不冒充当前生效版。
- 地图中心标记表示已保存位置，只支持缩放/失败重新加载，不能在只读页面选点或修改资料。窄屏自动上下排列，保留腾讯底图标识。
- 新只读接口由服务端选择已保存快照，不接受自由坐标；门诊管理员、可读该门诊的渠道账号、平台才能读取。未知/跨机构门诊和不属于该门诊的申请拒绝；员工不能借只读接口绕开现有门诊资料管理员权限。生效版本变化时要求刷新，不把新坐标配旧地址。既有编辑、资料重审及退回保留原版规则不变，无迁移。

## 服务器验证

以下路径均相对`/home/ubuntu/ChiHuiTong/test-results/`，所有运行和浏览器检查均在140.143.125.233：

- `20260913T034711Z-144e716dd016-backend/summary.json`：175项测试通过，覆盖率89%；lint/格式、迁移检查、HTTP启动及隔离备份恢复通过。新增快照来源、跨机构/员工拒绝、禁止传坐标、过期版本、无位置和服务失败覆盖。发布版Python业务代码与此版本一致。
- `20260913T034958Z-659eb05bab69-web/summary.json`：四角色完整业务回归通过；`map-view-checks.json`覆盖无定位、待审、批准、退回、双版对照、窄屏及失败重试。写入只发生在随机隔离库，结束删除本次临时库；地图传输使用明确的测试图片，不冒称腾讯真实服务验证。发布前端仅随后执行Prettier格式化，未改业务逻辑。
- 首轮失败是测试脚本对带重试按钮的错误文案使用了不合适的精确文本定位；修正为定位当前生效地图错误区域，最终整套回归通过，不忽略失败断言。
- `20260913T035506Z-live-profile-maps/summary.json`：已部署真实HTTPS平台、渠道、门诊三个角色的真实底图、详情和历史审核对照通过，Key未进入浏览器、脚本错误0、768px无整页溢出。验收时申请已完成审核，因此使用历史申请对照验证，不为测试重开或新建用户申请。实际平台对照截图已视觉核对，原版无定位空态、新版真实底图及状态清晰。
- `scripts/verify_live_profile_maps.py`只读取资料和地图，不发出资料提交/审核请求；地图调用会消耗实际腾讯额度。连续验证码登录测试须间隔至少60秒。

## 部署与恢复

- 应用版本`df52809dcdbff107832f7d74efa48ae0746e858e`；独立受保护HTTPS入口不变，Key/账号/附件/业务资料保留。
- 沿用现有`deploy_acceptance.py`先备份`runtime/acceptance/before-20260913T035317Z.dump`再升级本项目服务；无迁移、无新增公网端口，专属路由检查后平滑重载，其他共享服务保持active。共享Nginx原有重复MIME警告仍在，语法检查通过，本轮未顺带修改。
- 发布前后门诊profile、profile_version、review_status以及所有申请before/after/status摘要均为`8ea76cf580e83efab2edc0f10815ebd9af6ea4397f76e99a95646da8c0099b6b`；没有覆盖用户资料或替用户审核。
- 可回退到上一应用d4f39e71e4e005995820d25a96c795920a1537f5；无数据库恢复必要，不用旧dump覆盖之后的业务操作，不撤销腾讯Key配置。后续文档/脚本提交不改变已验证运行版本。

限制：不宣称生产验收或真实地理精度自动保证；地点仍由门诊/渠道人工核对并由平台审核。只读显示不改变当前推荐排序及既有预约。平台需要维护地图额度；既有地理编码对服务商非零状态的分类提示优化不在此次范围。
