# 腾讯地图接入与验证记录

REQ-032 / TASK-095 / ADR-0011、0028。2026-09-13，仅开发服务器140.143.125.233；非生产上线验收。

## 配置与验证结果

- 用户提供WebService Key，源文件`.secrets/tencent-map.env`已被Git忽略。Key不进入源码、命令行、浏览器或公开报告。
- `scripts/configure_acceptance_map.py`在指定服务器以ubuntu执行：锁定部署、公开地址预检，成功后备份并原子更新私有配置，只重启`chihuitong-acceptance.service`；应用版本仍为d4f39e71e4e005995820d25a96c795920a1537f5。真实HTTPS健康检查通过。
- 公开测试地址“北京市海淀区中关村大街1号”：地址解析得到GCJ-02候选，真实PNG底图600×360、266696字节，保留腾讯标识。仅证明接口可调用，候选位置是否准确仍需人工地图核对；不代表某家门诊已确认位置。
- `test-results/20260913T030246Z-map-activation/summary.json`记录配置成功；同目录`public-place-map.png`已人工查看。报告路径均相对服务器项目根目录。
- 页面验证未通过：`test-results/20260913T030843Z-map-browser/summary.json`记录渠道新增门诊表单地址解析HTTP409。两种公开地址拼接的直接诊断均收到腾讯业务状态121；这是日调用量达上限，不是本项目鉴权失败，也不能据此认定地址错误。见[腾讯官方状态码](https://lbs.qq.com/service/webService/webServiceGuide/status)。已停止调用，不绕过配额、不购买额度、不擅自换Key。
- 另一次快速重跑在登录阶段超时，验证码60秒获取限制仍保持；后续测试应间隔至少60秒，不为测试放宽认证限制。
- 配置与页面验证脚本在服务器执行ruff E9/F检查通过。本项目及原有公共服务、Study System、Nginx均active；未改共享配置。

## 待完成与已知限制

后续进展：用户已分配额度，直接接口状态恢复为0；真实页面地址解析也恢复HTTP200。首次页面底图检查遇到测试脚本在图片解码完成前断言的时序问题，已改为等待图片加载完成，正在复验；不将该断言失败误报为腾讯服务失败。

1. 用户在腾讯控制台检查该Key的“地址解析（geocoder）”和“静态地图（staticmap）”免费日额度及QPS分配。尚未查看账户控制台，不能判断是未分配还是已用完。
2. 额度可用后，在服务器用`.venv/bin/python current/scripts/verify_acceptance_map.py`复验真实HTTPS渠道地址定位、底图点选坐标变化、确认回填及取消不保存、资源方地图接口403、浏览器不暴露Key；尚未执行到这些后续断言，不能标记通过。
3. 现有应用将腾讯非零地理编码状态统一展示为“地址未能解析”。本次没有修改业务应用；配额/鉴权错误的分类友好提示仍需后续完善。
4. 本轮未保存门诊资料、未验证门诊资料再次审核闭环、未做小程序测试，未跑完整后端测试套件。不得把接口预检当作端到端或生产验收。

## 安全配置流程与回退

Key文件只传输至服务器`runtime/acceptance/map-key-input.env`，父目录0700、文件0600；使用`.venv-backend/bin/python current/scripts/configure_acceptance_map.py`。脚本仅接受一行`CHT_TENCENT_MAP_KEY=...`配置，预检失败不覆盖配置；更新后健康检查失败自动恢复原文件。该脚本会真实请求地图接口，恢复配额前不要重复执行。

更新前备份为`environment.json.before-map-20260913T030246Z`和`service.env.before-map-20260913T030246Z`，只存在服务器私有运行目录。服务器本次一次性传输文件已删除（可从用户保留的本地私有文件重新传输）；本地原文件未删除。回退只需安全恢复本次变更前的两个私有文件并重启本项目服务，但恢复前须检查是否有后续其他配置变更，不可用旧整份配置覆盖新值。无数据库和Nginx回退事项。
