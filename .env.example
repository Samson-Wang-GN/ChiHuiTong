# No application secrets or environment variables are required by the current
# static prototype. Real backend settings will be added with its architecture.
# SSH keys and GitHub credentials stay outside this repository.
