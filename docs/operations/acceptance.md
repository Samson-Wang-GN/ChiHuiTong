# 后端验收环境

REQ-045 / TASK-090 / ADR-0028（沿用REQ-044独立部署保护）。仅在开发服务器140.143.125.233运行和验证，非生产环境。

## 地址和登录

已部署入口 `https://dev-public.chihui-ai.com/chihuitong/`。四个独立入口：`platform/`、`resource/`、`channel/`、`clinic/`，分别使用合成手机号13800000001～04。第一层为独立HTTP Basic访问口令，私有保存于服务器 `runtime/acceptance/access-handoff.txt`，已私下交付到本地被Git忽略的`.secrets/acceptance-access.txt`，不提交仓库。进入角色页后获取随机验证码，再点“查看测试短信箱”，手动输入并登录；刷新重新登录，不在localStorage保存会话。

测试短信箱受入口口令和内部代理令牌共同保护，验证码5分钟有效、60秒获取间隔、一次性消费及失败次数限制。默认种子为上述四个号码；REQ-045显式模拟验收启用后，后台新开通的有效合成账号也可使用测试短信箱，便于验证管理员/业务员范围。不能用于真实用户手机号验证；生产禁止此模式。停用入口时停止专属服务并移除专属Nginx引用；更换入口口令需更新私有access.json并重部署，禁止将口令传入命令行或写入日志。

## 本次范围

2026-09-16最新应用：`675696e545d57918ef0e70c61caec16c588de46c`，REQ-051支持待签草稿、平台模板配置、生成PDF/下载打印、签署照片回传与版本校验；门诊合同仅平台与门诊签约主体双方。迁移0020只增表。204项后端及四角色业务/合同回归、真实HTTPS`20260916T061312Z-acceptance-browser`通过。发布前后1098条核心ID、私有环境配置及共享站点不变；模板表为0条，需平台配置认可的正式正文。数据库备份`runtime/acceptance/before-20260916T061237Z.dump`，其他私有回退资料位于`runtime/acceptance/release-backups/675696e545d57918ef0e70c61caec16c588de46c/`。操作与限制见[合同生成交付](../contract-generation-review.md)。

上一轮应用：`9b737ec5fd7b7d1267bd3dc280e6e79c48718143`，REQ-048～050统一合同、单店联合入驻、连锁主体后付与单店现付后台。0017～0019迁移通过；发布前后1098条核心业务ID摘要相同。数据库备份`runtime/acceptance/before-20260916T050637Z.dump`；旧部署/应用和worker服务文件、私有配置/附件备份位于`runtime/acceptance/release-backups/9b737ec5fd7b7d1267bd3dc280e6e79c48718143/`，仅服务器私有权限保存。

真实HTTPS回归`20260916T050727Z-acceptance-browser/summary.json`通过：四角色登录/菜单/详情/窄屏/退出、入口401/绕过403、应用重启及原站点检查。197项后端、四角色完整业务回归均在发布前通过。支付/短信仍为显式模拟，既有地图私有Key及用户资料保留。旧程序不识别新收款/主体模型，有新写入时禁止直接还原旧库；详细回退边界、测试及操作路径见[本轮交付](../contract-onboarding-review.md)。以下为历史版本，不能将df52809视作当前应用。

TASK-096最新应用：`df52809dcdbff107832f7d74efa48ae0746e858e`。门诊生效/待审定位提示、资料与审核对照真实地图已部署；无迁移，备份`runtime/acceptance/before-20260913T035317Z.dump`。门诊资料与全部申请内容摘要发布前后相同。三角色真实HTTPS报告`20260913T035506Z-live-profile-maps`通过，回退应用为d4f39e7（保留数据库及源码外地图Key）。后续文档/验证脚本提交不切换应用。完整证据见[地图页面交付](../clinic-map-review.md)。

TASK-095配置更新（2026-09-13）：腾讯地图Key已安全写入`runtime/acceptance/environment.json`及`service.env`，配置备份后缀`.before-map-20260913T030246Z`；仅重启本项目应用，代码版本仍为d4f39e7，无迁移。真实地址解析与底图预检成功；用户分配额度后，`20260913T031651Z-map-browser`聚焦页面验证通过。详见[地图配置、回退及验收证据](../tencent-map-acceptance.md)。

TASK-094最新应用：`d4f39e71e4e005995820d25a96c795920a1537f5`，Excel识别/校验同步处理，其他任务仍按原timer执行。升级前备份`runtime/acceptance/before-20260912T104359Z.dump`；79条核心业务ID摘要不变，无数据库迁移。真实HTTPS回归`20260912T104425Z-acceptance-browser`通过。上一个可回退应用为下方1ccf62b；后续文档提交不切换已验证应用。

TASK-092后续修正：当前应用1ccf62b5c4fcbc37d9c37b41f508baa94e0d0cc1，按真实Excel结构计算行列，修复无dimension模板被误记0×0。旧未提交批次在下一步时兼容修复，不重置用户记录；下段19b4c00为上一发布。

最新REQ-046（后于下段REQ-045初始交付）：运行应用19b4c00ad5fa2df48984b38fd47fc66a6a373b3f，Excel上传自动匹配及一次下一步已部署。真实HTTPS回归`20260912T050512Z-acceptance-browser`通过，65条核心业务ID保留；其他数据和访问保护不变，详见[Excel交付](../excel-upload-review.md)。

2026-09-12已升级完整Web，运行版本01475e059c8247a69d68143fd193624f3ba37f41。真实HTTPS回归`20260912T042111Z-acceptance-browser`通过：平台15、资源方9、渠道9、门诊8个可见菜单（共41），四角色脚本错误0、详情/退出/窄屏通过；原65条核心业务ID摘要一致，应用重启及原站点均正常。用户验收步骤见[验收指引](../web-acceptance-guide.md)。后续文档或辅助检查提交不会改变已部署应用版本。

REQ-045用完整业务界面替换原JSON轻量验收台：官方Arco表单/弹窗/抽屉、状态Tab、真实附件预览、Excel列适配、合同产品配置、预约核销、门诊/合作方收付款、账号、消息、概览及跨Tab提醒直接调用服务端。四角色共享真实业务数据但按服务端权限隔离；小程序和静态原型未改。具体测试及部署版本以[Web实施清单](../web-backend-plan.md)和服务器`runtime/acceptance/deployment.json`为准，不能把下面旧阶段证据当成本次完整验收。

演示包含4个管理员、3个合作/门诊合同、1个推广产品、记名/实体卡订单、客户及待领取/已领取权益、待确认/成功/实际核销/系统完成预约、过期预约待办、已结清和未结清门诊账单、两方待确认月度结算单。名称与附件均标记虚构，合成收款不是实际资金到账。演示预约和任务会随真实时间变化。

## 隔离和部署

- 独立数据库`chihuitong_acceptance`，仍位于本项目独立PG实例/socket，不连接共享5432。自动回归继续使用`test_chihuitong`，不会清空验收库。
- 私有配置、加密附件、访问资料、部署备份和报告位于`/home/ubuntu/ChiHuiTong/runtime/acceptance`，目录0700、文件0600；与代码release分离。
- `scripts/deploy_acceptance.py`只能从指定主机校验release执行：创建空库/迁移/初始化；已有种子标记不重置用户操作。非空无标记拒绝初始化；禁止强行flush或删库。
- `chihuitong-acceptance.service`只监听127.0.0.1:18243；`chihuitong-acceptance-worker.timer`每分钟执行任务。不打开新公网端口。
- 经用户授权，只向现有开发HTTPS站点增加一条专属location文件include，保留原站点路由及证书。Nginx执行语法检查后平滑reload；语法失败恢复原站点。部署记录原文件哈希、备份和共享服务前后状态。
- 真实微信接口强制关闭；`CHT_ACCEPTANCE_SIMULATED_EXTERNALS=true`启用显式模拟支付/短信。模拟支付仍写入验收业务账本并标记`wechat_simulated`，模拟短信记录以`SIMULATED-`识别，没有真实资金或短信外发。地图缺配置不可确认，不伪造真实底图。生产硬拒绝模拟开关。
- `initialize_simulation`只初始化尚未被修改的预置短信模板，不覆盖已有平台配置。前端会话不落localStorage；仅本机预约提醒偏好可保留。
- 自动化Web回归使用随机`chihuitong_acceptance_web_<12位十六进制>`库及127.0.0.1:18244临时网关，结束后只删除本次临时库；不清空持久验收库，不开放公网端口。

## 运维与回退

重复部署前保存验收库dump，不自动清理演示修改、历史备份和附件。附件及加密密钥应与数据库一起备份，私有密钥不放Git。

应用重启不重置数据库。项目PG目前沿用独立开发实例的启动方式，整机重启后应先按后端运行器启动该实例再启动验收服务；不宣称已有整机重启自动恢复验收。服务自带故障重启，定时任务失败留服务日志。

入口回退：核对专属include内容和备份时间，移除本项目增加的include并nginx -t/reload；不直接以旧整文件覆盖后来新增的其他站点修改。停止/禁用本项目验收服务及timer，保留数据库/附件/密钥。代码回退须使用已测release并核对迁移兼容性；禁止删除验收库作为回退。

上线验证必须覆盖TLS真实证书校验、无口令401、直接绕过代理403、四角色随机验证码登录/越权拒绝、实际有演示数据、应用重启后数据不变、既有站点响应和服务不变。只在服务器运行浏览器测试。

## 早期轻量验收阶段证据（REQ-044）

2026-09-12部署应用`5b75779b068f792b5a367fc013d69444cad781a3`，验证脚本`48744b02b31c16801391916a2700a0dc038743ef`。报告`/home/ubuntu/ChiHuiTong/test-results/20260912T013848Z-acceptance-browser/summary.json`通过：四角色共24个菜单，真实验证码登录、详情和显式关闭、768px窄屏及退出，JS错误0；入口401/代理绕过403；应用重启后14条核心记录ID不变；既有根/Study HTTPS200。截图在同目录，未公开暴露测试产物。

后端151项回归及覆盖率88%见`20260912T012511Z-7ffad8c0b050-backend/summary.json`，后续仅验收展示/部署脚本和文档变化，Python业务及安全实现相同。上述通过不包括真实第三方、完整业务前端或生产验收。入口第一层凭据不随业务“退出账号”清除；退出业务会撤销Bearer会话，离开共用电脑应关闭浏览器会话或无痕窗口。
