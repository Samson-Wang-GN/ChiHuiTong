# 后端实施、验收覆盖与自主决策

状态：2026-09-12启动，尚未完成。需求0.32、REQ-043；技术见[ADR-0022](decisions/0022-backend-monolith-and-isolated-validation.md)。小程序与原型页面本轮不改。

## 实施清单

| 任务 | 覆盖 | 完成依据 |
| --- | --- | --- |
| TASK-081 | 工程、独立PG、迁移、加密、认证、机构身份、审计 | 服务器检查、认证/权限/并发测试 |
| TASK-082 | 机构/账号、来源名、产品、合同版本与分配、门诊资料审核/上下线 | 四角色API、租户/版本冲突测试 |
| TASK-083 | Excel异步映射/预览/格式、销售/付款审批、号段、卡/客户/权益、取消停止/退款 | 原子性、跨来源匹配、重复与并发 |
| TASK-084 | 预约/改期/核销/撤销、24/72小时、待恢复、固定码、结算状态 | 时间边界、数量守恒、权限及并发 |
| TASK-085 | 周/月账单、凭证审核、历史归集、合作方确认/付款/到账、Excel | 金额/版本/入单唯一性、历史未回款 |
| TASK-079 | 微信Native/JSAPI、签名验签/解密/查单/关单、资金协调 | 模拟契约、伪回调/竞态/缺配置 |
| TASK-086 | 待办、通知/发件箱、预约快照、权益概览/趋势/导出 | 全量筛选/角色隔离、事件时点回放 |
| TASK-087 | API、运维/备份恢复、威胁模型、综合安全/启动回归 | 指定服务器证据及限制 |
| TASK-088 | GitHub/服务器一致交付、决策/用户验收 | 已测提交、不可变快照、风险清单 |

## 自主决定（供用户审阅）

1. 不改小程序UI，但实现共同后端和客户闭环接口；不同登录端凭证受众隔离，普通客户不能访问门诊后台。
2. 独立PG实例/虚拟环境，不借用公共数据或更改共享服务。
3. 后台使用短期、可撤销不透明会话，服务端仅存摘要；验证码单次、短时、限速及限制失败次数，API/日志不返回验证码。测试注入短信适配器观察合成消息。
4. 当前负责关系只落销售订单/门诊，下游稳定外键解析；历史收益方/操作人不可变，不实现交接。
5. 新业务要求当前有效合同，存量履约回溯已审核生效过的适用版本；缺失明确拒绝，不默认零费用。
6. 预约提醒提供鉴权完整待确认快照及版本，建议前端每5秒轮询、重连重新取快照；只编号/意向时间/版本，不含患者资料。本轮不改浮窗。
7. 审核工作日使用可维护日历，周末为初始默认；节假日/调休需平台录入才承诺精度。付款期限始终自然日。

后续决定和测试结果随实现追加；未实现不得标完成。真实商户/短信/地图/小程序配置缺失仅限制对应真实联调，不取消代码及模拟异常测试。

## 分阶段验证记录

- 第一阶段提交`0856e39929cb4dfe5d62f8897f2b4df8d0f0ea06`：2026-09-12北京时间01:56起在VM-0-12-ubuntu执行；独立PG16初始化成功，依赖检查、lint/格式、初始迁移、框架检查和15项测试通过，阶段代码覆盖率89%。包括4并发相同幂等请求只执行一次、同时降级管理员仍保留一名、验证码失败次数提交及会话/身份越权。报告`/home/ubuntu/ChiHuiTong/test-results/20260911T175658Z-0856e39929cb-backend/summary.json`，交付快照未变化、四共享服务前后相同。
- 此次在测试副本生成初始迁移并机械格式化，已取回本地评审；最终源码需再次验证，不将该记录等同全部后端通过。TASK-081尚有生产认证适配/审计和综合验证，继续进行。
- 第二阶段`8a31cbf4eba21b36d7fb2af2a8d776e716c2cbd4`：29项测试通过，阶段覆盖率88%，包含数据库拒绝修改/删除审计事件；报告`/home/ubuntu/ChiHuiTong/test-results/20260911T181229Z-8a31cbf4eba2-backend/summary.json`。此前`8f92a76`格式检查发现lambda赋值，修正后复测，失败报告保留。
- 第三阶段`e4cc3b513c97e6fc82150900f0d51b375ddd91c6`：47项测试通过，阶段覆盖率87%；报告`/home/ubuntu/ChiHuiTong/test-results/20260911T183006Z-e4cc3b513c97-backend/summary.json`。包含Excel异步列确认/异常导出、审核前零客户写入、采购部分款不提前开卡、只补空且保留0岁、并发客户复用及领取/取消互斥。生成迁移和机械格式化文件已取回，尚需最终提交复测。共享服务状态相同、交付源码哈希不变。
- 第四阶段`5c4ed8595dc373ae5f3ad4a80b3838547a8307f6`：57项测试通过，当前总体覆盖率84%（新增财务服务尚未专项测试，覆盖率下降不视为已验收）；报告`/home/ubuntu/ChiHuiTong/test-results/20260911T184223Z-5c4ed8595dc3-backend/summary.json`。覆盖预约数量占用、不预收费、日期/改期约束、待确认超时、24/72小时、反馈冲突、普通和系统补核销撤销、到期后按预约日期补核销、已结算拒绝撤销。生成迁移/格式化已取回，GitHub与服务器同步至该提交，后续继续金融专项及接口完善。
- 附件采用10MB单文件保护，图片20MP限制并重新编码去掉EXIF，合同静态PDF最多200页并拒绝脚本、外链、嵌入文件；最多20附件/业务版本。它们是资源/安全限制，不是业务总量配额。pypdf6.18.1用于静态PDF结构检查（BSD许可证），不引入文档编辑功能；无法安全读取明确拒绝，不假称病毒扫描已完成。

## 待综合完善（不冒充完成）

- 工作台阶段`391dc18`远程102项测试中1项失败：多个无期限待办在PostgreSQL UNION中把NULL推断为文本，与后续时间列冲突。改为显式时间类型转换，并新增直接服务调用保留回归诊断；复测待执行。迁移0009由服务器生成后取回。
- 对账反馈口径修正：合作方在待确认阶段反馈后进入“有异议”，平台回复后恢复待确认，不能跳过反馈确认付款；门诊反馈不阻断付款，均不改变金额及截止日。
- REQ-029正式接口采用开卡批次圈选，历史事件逐时点回放，数据库禁止修改/删除事件。默认近30天批次；观察时点与事实入账截止时间可复用于顶部、趋势、明细和导出。当前客户姓名等是授权范围内的最新资料，不能冒充历史姓名。趋势单次最多366个点，可改按周/月或分段；这是查询保护，不是机构/客户总量限制。
- 概览仅平台/资源方开放，业务员按销售订单负责人；跨资源方、跨产品人数均按稳定客户ID去重。卡片列表按观察时点未领取/激活/作废Tab，产品按当前启用状态Tab；核销不依赖门诊收款。导出包含当前筛选全部记录。搜索先支持精确系统客户编号、手机号HMAC或卡号，不对加密姓名做明文SQL模糊查询。真实历史缺少开卡事件的记录单列未纳入数量，不补造历史。

- 通知阶段`4c6a9fa`远程97项测试通过，报告`/home/ubuntu/ChiHuiTong/test-results/20260911T192203Z-4c6a9fa6cc0a-backend/summary.json`；GitHub已由本地推送至此版本。短信未真实发送，小程序页面未变更。
- 对照REQ-002及机构原型补齐机构“待审核/退回”流程：平台录入时建受保护管理员，但机构审核通过前身份不可使用；联系人默认取初始管理员，可由平台独立修改，不联动账号手机。不能用启用按钮跳过审核。合同审核同时校验主体资质；门诊合同仍由渠道提交、平台审核。
- 工作台采用按权限合并的业务查询，不复制一份可随意标完成的任务状态；独立任务页显示真实记录及可操作动作，处理结果来源于原业务接口。对象日志新增操作时角色/名称快照；旧历史缺失字段显示未记录，不用当前角色伪造过去身份。

- 微信阶段`578631d`在指定服务器89项回归通过，覆盖可信报文、延迟回调、资金唯一流水、并发下单不持有账单锁；报告`/home/ubuntu/ChiHuiTong/test-results/20260911T191212Z-578631dc773f-backend/summary.json`。真实商户、回调域名和JSAPI登录身份联调仍待后续。
- 通知默认：站内“已读”不改变业务待办。模板仅平台配置预置种类、变量和已报备模板ID；初始化不覆盖现有配置，默认不发送。选用腾讯云短信2021-01-11接口，TC3签名；服务商接受不冒充手机已收到。网络未决有限重试，外部短信不承诺严格一次送达。凭据未配置时保留失败/重试证据和工作台提醒。
- 付款短信使用已生效门诊业务联系人电话和当前渠道负责业务员绑定手机；联系人若填座机无法发送短信，将明确失败供平台修正资料，不暗中改发其他私人手机号。此配置注意事项需上线前核对。
- 调度每分钟执行、任务分别持久化和重试；周一/每月1日出账，历史未入账交易始终保留到后续账期。服务停机错过调度日期时暂不伪造当日已出账事实，平台需核查；综合运维阶段继续补停机恢复证据。

- `ab68b84`远程74项测试通过，包含前次两处修复及预约API/费用签名/关闭条款存量履约。报告`/home/ubuntu/ChiHuiTong/test-results/20260911T190204Z-ab68b84b4a41-backend/summary.json`。本轮继续实现微信适配及支付并发，不将缺商户配置当作不开发支付的理由。

- `b8d2c1e`远程70项回归中两项失败：非对象JSON错误结构引发500，以及模拟未来账期时测试客户端会话已过期。已分别修正校验错误结构与测试会话建立时点，等待下一轮回归；失败报告保留在`20260911T185621Z-b8d2c1e9f3c2-backend`。
- 扫码确认增加5分钟签名费用报价，接口只展示门诊费用，分配明细仅以摘要绑定，不泄露合作方分配；核销时再次校验预约版本和当前计费快照，变化后必须重新确认。停用合同中的推广产品仅阻断新业务，存量预约仍可按最近适用条款履约。

- 财务服务阶段`9952c3253635780ee78a7d5f2c1cfa0c16db4a68`远程回归通过，总体覆盖率89%；报告`/home/ubuntu/ChiHuiTong/test-results/20260911T184551Z-9952c3253635-backend/summary.json`。当前继续账单API、逐笔Excel导出、整单/本人明细隔离与反馈处理测试，尚非最终交付。
- 对账反馈默认不改变账期、金额或资金状态，仅形成平台回复待处理记录；门诊争议展示不自动下线，不阻断正常付款。合作方整单由管理员处理，业务员仅通过本人明细接口获取本人行，不返回整单总额。

- 已实现批次异步校验，进度当前按整次任务更新；大批开卡需继续验证时长及异步审批、失败恢复，不能将10万张技术保护值作为已压测容量。
- 后台当前已有显式核心接口；仍需补完整对象日志、机构资料更新、合同版本显示/终止、真实短信/手机号授权/地图适配和接口文档。
- 预约、财务、通知和概览仍在实施；真实支付及商户联调未进行。

补充工程默认：预约及改期按北京时间的日期判断权益有效期；补核销操作时间独立保存。门诊存在已确认部分到账时，如果撤销某交易会使实收超过修订后应收，先阻断并提示平台核对，不静默吞掉实收或自动退款；若修订后剩余金额恰好已全额到账，则结清剩余有效交易。0获客费门诊账单暂记“无需付款”而不伪造实际收款；它不会因零金额而自动冒充满足合作方实际回款归集前提，需在综合验收记录此边界。
