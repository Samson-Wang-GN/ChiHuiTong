# REQ-046 Excel上传简化交付

## TASK-092 模板维度兼容修正

用户反馈“下一步”提示工作表/表头错误，经只读检查：上传xlsx实际3行4列、预览3行且自动识别表头1，但没有可选dimension元数据，旧代码用`max_row or 0`错误缓存为0×0，后端拒绝任何表头。不是用户缺少选择步骤；常规有维度的测试样本未覆盖此种合法模板。

修复按实际worksheet XML的行/单元格位置有界计算维度，忽略缺失/低报/高报的维度声明；预览和全表读取使用同一实际范围。沿用50MiB展开上限、20表、10020行/200列保护，先检查实际坐标再让读取器遍历，不能通过省略dimension绕过上限。openpyxl固定版本的内部worksheet归档路径仅封装在`open_book`边界，升级依赖须复跑专项。

已上传但未提交订单的旧0×0缓存，在用户再次点击“下一步”的授权POST中重新读取并随同配置提交修正；GET不修改业务数据，已提交订单的依据不可重写。无需人工指定工作表/表头，也无需强制重新上传。仅当自动结果有误才展开设置纠正。不会替用户提交订单、开卡或审核。

修复版对用户原文件只读解析结果为3×4、表头1、3项必要字段全部自动匹配；未改该批次或任何客户数据。新增合法无dimension模板的页面流程，以及缺失/低报/高报维度、真实坐标超限、旧批次下一步恢复的后端回归。最终证据见TASK-092。

后端167项通过/覆盖率89%：`/home/ubuntu/ChiHuiTong/test-results/20260912T095824Z-dc3b32ade795-backend/summary.json`；模板与全部Excel专项：`/home/ubuntu/ChiHuiTong/test-results/20260912T095839Z-dc3b32ade795-web/summary.json`通过。应用1ccf62b5c4fcbc37d9c37b41f508baa94e0d0cc1已部署，格式化后服务文件与该测试副本SHA-256相同；无迁移，旧版回退候选19b4c00，保留私有数据库备份及原文件。

部署后`/home/ubuntu/ChiHuiTong/test-results/20260912T100251Z-acceptance-browser/summary.json`通过，业务数据保留。原上传页可直接再点下一步；若已离开未提交向导，重新上传同一文件即可自动识别，不要求修改Excel或手工指定表头。

## 交互与决定

- 只改正式客户资源方后台记名批量销售；实体卡、单客、付款、平台审核与小程序不变。
- 上传自动触发读取，不再要求“读取Excel”“识别”“校验”“确认导入”四次操作。底部一次下一步代表人工确认当前映射，服务端仍记录校验版本/摘要/确认时间。
- 先展示原文件列头和表头后最多10行（含原行号，不足10行全部显示），再核对3个必需字段。可选字段和工作表/表头设置折叠。预览中空白行保持原位置，整批校验仍按既有规则跳过全空行。
- 自动识别只使用明确别名和本机构已确认相同表头结构；不根据姓名/手机号样本猜测字段、不传外部AI。两个合理手机号列、重名列或已保存格式冲突均要求人工选择。数量无默认值；统一数量必须主动输入。
- 多个同分客户工作表默认选原文件顺序第一个，同时提示核对；保留手动设置。保存机构格式移到已提交订单的“Excel导入依据”，不增加主流程按钮。
- 读取/校验仍在独立后台任务执行，页面自动显示等待并轮询；90秒未完成显示稍后再试，不重复上传。切换文件/表头时丢弃旧请求响应，不能复用旧确认。失败只保留草稿批次，不生成客户/卡/权益。
- Arco Collapse接口按[官方定义](https://raw.githubusercontent.com/arco-design/arco-design/main/components/Collapse/interface.ts)核对，运行组件仍使用项目固定2.66.16版本，不升级依赖。

## 测试证据

全部测试仅在140.143.125.233运行。后端报告：`/home/ubuntu/ChiHuiTong/test-results/20260912T045756Z-b3d7431f9a22-backend/summary.json`，164项通过，覆盖率89%，包含新增4项自动匹配测试；迁移/lint/格式/启动/备份恢复/共享服务和源码不变检查通过。

Excel页面专项：`/home/ubuntu/ChiHuiTong/test-results/20260912T045721Z-b3d7431f9a22-web/summary.json`与`excel-focused.json`通过，覆盖第20行表头、多表、12条只预览10条、一次下一步、返回修改统一数量重新计算、缺必需列、同列重复绑定、无默认数量、全表异常、删除替换旧文件失效及768px。桌面/窄屏截图已目视核对。

首次页面回归因测试计数误含表头行失败（并非多出客户数据），已改为只计算tbody并复跑通过。后续增加快速修改表头时不卸载输入的修正，完整回归`/home/ubuntu/ChiHuiTong/test-results/20260912T045949Z-19b4c00ad5fa-web/summary.json`通过，包含原17条跨角色流程、新增4组Excel专项、快速重选表头和真实PiP。应用已部署`19b4c00ad5fa2df48984b38fd47fc66a6a373b3f`；后端Python与上述164项测试版本一致，后续只改前端及验证脚本。

## 发布边界

部署后真实HTTPS报告`/home/ubuntu/ChiHuiTong/test-results/20260912T050512Z-acceptance-browser/summary.json`通过：四角色、新Excel入口、41个菜单、越权拒绝、窄屏、退出及应用重启。8类核心业务对象共65条ID摘要升级前后一致，访问口令不变。辅助验证及文档提交86332e0不改变运行应用19b4c00；GitHub及服务器文档同步以最终交付提交为准。

无数据库迁移。保留持久验收库、访问口令和订单；自动化写入只在临时独立库。按原授权部署同一受保护HTTPS入口，回退可使用上一已测应用01475e0及现存私有备份，不删除新批次或历史记录。真实支付/短信/地图等边界不变。
